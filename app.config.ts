export default defineAppConfig({
    notifications: {
      position: 'top-0 right-0'
    },
    nuxtIcon: {
      size: '24px', // 设置默认图标大小
      class: 'icon', // 添加默认类名
    },
    icons: ['ph']
})
